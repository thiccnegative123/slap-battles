local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")

local LocalPlayer = Players.LocalPlayer
local CameraEvent = ReplicatedStorage:WaitForChild("Events"):WaitForChild("camera")


-- GUI
local gui = Instance.new("ScreenGui")
gui.Name = "loop TargetGui"
gui.ResetOnSpawn = false
gui.Parent = LocalPlayer:WaitForChild("PlayerGui")


local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 300, 0, 260)
frame.Position = UDim2.new(0.5, -150, 0.75, 0)
frame.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
frame.BorderSizePixel = 0
frame.Parent = gui


local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = frame



-- Dragging
local dragging = false
local dragStart
local startPosition


frame.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = true
		dragStart = input.Position
		startPosition = frame.Position
	end
end)


frame.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = false
	end
end)


UserInputService.InputChanged:Connect(function(input)
	if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
		local delta = input.Position - dragStart
		frame.Position = UDim2.new(
			startPosition.X.Scale,
			startPosition.X.Offset + delta.X,
			startPosition.Y.Scale,
			startPosition.Y.Offset + delta.Y
		)
	end
end)



-- Title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 35)
title.BackgroundTransparency = 1
title.Text = "Loop Target Player"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 18
title.Parent = frame



-- Username box
local box = Instance.new("TextBox")
box.Size = UDim2.new(0.85, 0, 0, 35)
box.Position = UDim2.new(0.075, 0, 0.14, 0)
box.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
box.PlaceholderText = "Username... (type 'all' for everyone)"
box.Text = ""
box.TextColor3 = Color3.fromRGB(255, 255, 255)
box.Font = Enum.Font.Gotham
box.TextSize = 14
box.Parent = frame


local boxCorner = Instance.new("UICorner")
boxCorner.CornerRadius = UDim.new(0, 8)
boxCorner.Parent = box



local function createButton(text, pos, size)
	local button = Instance.new("TextButton")
	button.Size = size or UDim2.new(0.4, 0, 0, 35)
	button.Position = pos
	button.BackgroundColor3 = Color3.fromRGB(70, 120, 255)
	button.Text = text
	button.TextColor3 = Color3.fromRGB(255, 255, 255)
	button.Font = Enum.Font.GothamBold
	button.TextSize = 13
	button.Parent = frame

	local c = Instance.new("UICorner")
	c.CornerRadius = UDim.new(0, 8)
	c.Parent = button

	return button
end



-- Front button (top left)
local frontButton = createButton(
	"Front 8",
	UDim2.new(0.07, 0, 0.38, 0)
)


-- Back button (top right)
local backButton = createButton(
	"Behind 8",
	UDim2.new(0.53, 0, 0.38, 0)
)

-- Orbit button (middle left)
local orbitButton = createButton(
	"Orbit",
	UDim2.new(0.07, 0, 0.62, 0)
)

-- Linespamm button (middle right)
local linespammButton = createButton(
	"Linespamm",
	UDim2.new(0.53, 0, 0.62, 0)
)



-- Orbit positions - UNIFORM 19 stud radius (centered on player, 5 studs further)
local orbitPositions = {
	{pos = Vector3.new(0, 0, -19), name = "Front"},
	{pos = Vector3.new(13.44, 0, -13.44), name = "Front-Right"},
	{pos = Vector3.new(19, 0, 0), name = "Right"},
	{pos = Vector3.new(13.44, 0, 13.44), name = "Back-Right"},
	{pos = Vector3.new(0, 0, 19), name = "Back"},
	{pos = Vector3.new(-13.44, 0, 13.44), name = "Back-Left"},
	{pos = Vector3.new(-19, 0, 0), name = "Left"},
	{pos = Vector3.new(-13.44, 0, -13.44), name = "Front-Left"},
}

-- Linespamm positions - 5 lines CLOSER TOGETHER
local linespammPositions = {
	Vector3.new(-20, 0, -14),  -- Far Left
	Vector3.new(-10, 0, -14),  -- Left
	Vector3.new(0, 0, -14),    -- Center
	Vector3.new(10, 0, -14),   -- Right
	Vector3.new(20, 0, -14),   -- Far Right
}

local orbitLoop = false
local linespammLoop = false
local currentOrbitIndex = 1



-- Find player using partial name
local function findPlayers(input)
	input = string.lower(input):gsub("^%s+", ""):gsub("%s+$", "")
	local matches = {}

	if input == "me" then
		table.insert(matches, LocalPlayer)
		return matches
	end

	if input == "all" then
		for _, player in ipairs(Players:GetPlayers()) do
			if player ~= LocalPlayer then
				table.insert(matches, player)
			end
		end
		return matches
	end

	if #input == 0 then
		return matches
	end

	for _, player in ipairs(Players:GetPlayers()) do
		if player ~= LocalPlayer then
			local username = string.lower(player.Name)
			if string.sub(username, 1, #input) == input then
				table.insert(matches, player)
				break
			end
		end
	end

	return matches
end



-- Target function
local function targetPlayer(offset)
	local targets = findPlayers(box.Text)

	for _, target in ipairs(targets) do
		if target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
			local hrp = target.Character.HumanoidRootPart
			local position = hrp.CFrame * CFrame.new(0, 0, offset)

			CameraEvent:FireServer({
				cf = position
			})
		end
	end
end



-- Linespamm function
local function targetLinespamm()
	local targets = findPlayers(box.Text)

	for _, target in ipairs(targets) do
		if target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
			local hrp = target.Character.HumanoidRootPart
			
			for _, offset in ipairs(linespammPositions) do
				local position = hrp.CFrame * CFrame.new(offset.X, offset.Y, offset.Z)
				
				CameraEvent:FireServer({
					cf = position
				})
			end
		end
	end
end



-- LOOP SYSTEM

local frontLoop = false
local backLoop = false


local function startLoop(offset, check)
	while check() do
		targetPlayer(offset)
		task.wait()
	end
end


local function startOrbitLoop(check)
	while check() do
		local targets = findPlayers(box.Text)
		local currentPos = orbitPositions[currentOrbitIndex]
		
		for _, target in ipairs(targets) do
			if target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
				local hrp = target.Character.HumanoidRootPart
				
				local offset = currentPos.pos
				local cameraPos = hrp.Position + (hrp.CFrame.RightVector * offset.X) + (hrp.CFrame.UpVector * offset.Y) + (hrp.CFrame.LookVector * offset.Z)
				
				local direction = (cameraPos - hrp.Position).Unit
				local lookAwayCF = CFrame.new(cameraPos, cameraPos + direction)

				CameraEvent:FireServer({
					cf = lookAwayCF
				})
			end
		end
		
		orbitButton.Text = "Orbit: " .. currentPos.name
		task.wait()
		
		if check() then
			currentOrbitIndex = currentOrbitIndex - 1
			if currentOrbitIndex < 1 then
				currentOrbitIndex = #orbitPositions
			end
		end
	end
	orbitButton.Text = "Orbit"
end


local function startLinespammLoop(check)
	while check() do
		targetLinespamm()
		task.wait()
	end
end



frontButton.MouseButton1Click:Connect(function()
	if frontLoop then
		frontLoop = false
		frontButton.Text = "Front 8"
		return
	end

	backLoop = false
	orbitLoop = false
	linespammLoop = false
	backButton.Text = "Behind 8"
	orbitButton.Text = "Orbit"
	linespammButton.Text = "Linespamm"

	frontLoop = true
	frontButton.Text = "Stop Front"

	task.spawn(function()
		startLoop(-14, function()
			return frontLoop
		end)
	end)
end)



backButton.MouseButton1Click:Connect(function()
	if backLoop then
		backLoop = false
		backButton.Text = "Behind 8"
		return
	end

	frontLoop = false
	orbitLoop = false
	linespammLoop = false
	frontButton.Text = "Front 8"
	orbitButton.Text = "Orbit"
	linespammButton.Text = "Linespamm"

	backLoop = true
	backButton.Text = "Stop Behind"

	task.spawn(function()
		startLoop(8, function()
			return backLoop
		end)
	end)
end)



orbitButton.MouseButton1Click:Connect(function()
	if orbitLoop then
		orbitLoop = false
		orbitButton.Text = "Orbit"
		return
	end

	frontLoop = false
	backLoop = false
	linespammLoop = false
	frontButton.Text = "Front 8"
	backButton.Text = "Behind 8"
	linespammButton.Text = "Linespamm"

	orbitLoop = true
	currentOrbitIndex = 1
	orbitButton.Text = "Orbit: Front"

	task.spawn(function()
		startOrbitLoop(function()
			return orbitLoop
		end)
	end)
end)



linespammButton.MouseButton1Click:Connect(function()
	if linespammLoop then
		linespammLoop = false
		linespammButton.Text = "Linespamm"
		return
	end

	frontLoop = false
	backLoop = false
	orbitLoop = false
	frontButton.Text = "Front 8"
	backButton.Text = "Behind 8"
	orbitButton.Text = "Orbit"

	linespammLoop = true
	linespammButton.Text = "Stop Spam"

	task.spawn(function()
		startLinespammLoop(function()
			return linespammLoop
		end)
	end)
end) 
